"""Infrastructure layer — concrete implementations of domain abstractions.

Everything that touches external systems (USB HID, filesystem, OS, third-party
libraries) lives here. Depends on domain; never the other way around.
"""
